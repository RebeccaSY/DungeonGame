//package unsw.dungeon;

/**
 * A class interface to represent goals (or win conditions) in the game
 * @author stephen
 *
 */
public interface Goal {
	public boolean isComplete(); //returns true if the goal is achieved
}